# inLimbo::defs

Contains the X-Macro definition files that are used as a SSOT (single source of truth) for tasks like:

1. Generating structs / enums
2. Validation of types and such
3. Easy dynamic setup without refactoring too much of the code

## Usage

1. `args`: Used for arguments to generate the `inlimbo::Args` struct and the 
